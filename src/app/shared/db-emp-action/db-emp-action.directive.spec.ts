import { DbEmpActionDirective } from './db-emp-action.directive';

describe('DbEmpActionDirective', () => {
  it('should create an instance', () => {
    const directive = new DbEmpActionDirective();
    expect(directive).toBeTruthy();
  });
});
